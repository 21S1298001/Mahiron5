version: "2"
sql:
  - engine: "sqlite"
    schema: "db/schema.sql"
    queries: "db/queries/"
    gen:
      go:
        package: "gen"
        out: "db/gen"
        emit_json_tags: true
        emit_pointers_for_null_types: true
